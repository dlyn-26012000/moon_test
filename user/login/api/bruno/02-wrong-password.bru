meta {
  name: 02 Wrong password (BUG-001 expect 401 gets 500)
  type: http
  seq: 2
}
post {
  url: {{baseUrl}}/user/auth/login
  body: json
  auth: none
}
headers {
  Content-Type: application/json
  Accept: application/json
}
body:json {
  {
    "username": "{{username}}",
    "password": "definitely-wrong"
  }
}
tests {
  test("documents BUG-001: business error returns 500 not 401", function() {
    expect([401,500,429]).to.include(res.getStatus());
  });
}
